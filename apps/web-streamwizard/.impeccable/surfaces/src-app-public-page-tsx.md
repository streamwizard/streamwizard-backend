---
version: 1
slug: "src-app-public-page-tsx"
primary_target: "src/app/(public)/page.tsx"
related_targets: ["src/components/public/home/studio-hero.tsx"]
---

# Surface brief: homepage `/` (hero-led)

**Scope & mode.** The `(public)` homepage, hero first; Persuade. The hero carries the new world ("The Studio"); the spine sections below are still Signal Path until their follow-up rebuild.

**Audience & job.** All Twitch streamers evaluating tools between streams. Belief within seconds: my whole setup, one free open-source toolkit.

**Action.** Primary: Connect Twitch, seated in the Start Streaming slot of the controls dock. Secondary: Star on GitHub.

**Proof & content.** OBS-grammar studio chrome itself; real dashboard screenshot (`public/img/landing-page/hero-dark.webp`) as the program-monitor scene; real facts only (MIT, four pillars); all synthetic telemetry labeled demo data.

**Constraints.** New marketing lines ship as `[COPY: ...]` placeholders; existing shipped headline and IRL line may be reused. No invented metrics, customers, or prices. Red spent on LIVE only.

**Chosen direction.** The Studio (seed 77df92fe), code-led. Memorable moment: scene buttons switch the program monitor with a studio-mode cut; the headline sits in a selected-source bounding box with corner handles.

**Unresolved.** Rest of homepage (spine sections, GoLiveClose) still Signal Path; rebuild to The Studio planned as follow-up. Old `home/hero.tsx` retired.
