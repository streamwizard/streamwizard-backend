# Use streamwizard-dev / streamwizard-staging / streamwizard-prod to match
# the Doppler config's environment (dev_web_admin, staging_web_admin, ...)
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=
INFLUXDB_ORG=streamwizard
INFLUXDB_BUCKET=streamwizard-prod

# Supabase (Doppler names — derived to NEXT_PUBLIC_ at runtime)
SUPABASE_URL=
SUPABASE_PUBLIC_KEY=
SUPABASE_SECRET_KEY=

# WS Live Monitor (optional)
NEXT_PUBLIC_WS_SERVER_URL=ws://localhost:8000
NEXT_PUBLIC_MONITOR_SECRET=
