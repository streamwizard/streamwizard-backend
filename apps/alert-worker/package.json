{
  "name": "@repo/alert-worker",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "doppler run --config dev -- bun --preload ./src/sentry.ts --hot src/index.ts",
    "start": "bun --preload ./src/sentry.ts src/index.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/alerting": "workspace:*",
    "@repo/sentry": "workspace:*",
    "@sentry/bun": "^10",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "@types/node": "^20",
    "@repo/typescript-config": "workspace:*",
    "typescript": "5.9.2"
  }
}
