{
  "name": "@repo/ws-server",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "doppler run --config dev_ws_server -- bun --preload ./src/sentry.ts --hot src/index.ts",
    "start": "bun --preload ./src/sentry.ts src/index.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/metrics": "workspace:*",
    "@repo/sentry": "workspace:*",
    "@repo/supabase": "workspace:*",
    "@repo/types": "workspace:*",
    "@sentry/bun": "^10",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "^1.3.6",
    "@types/node": "^20",
    "typescript": "5.9.2"
  }
}
