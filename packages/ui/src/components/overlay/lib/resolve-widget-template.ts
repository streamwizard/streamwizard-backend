/**
 * Kept as the historical import path for the widget template layer, which now
 * lives in two files: the field schema helpers and the srcdoc builder.
 */

export * from "./widget-fields";
export * from "./widget-srcdoc";
